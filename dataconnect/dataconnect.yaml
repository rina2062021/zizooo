specVersion: "v1"
serviceId: "zizo"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "zizo-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
